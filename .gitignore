.venv/
__pycache__/
*.pyc
.pytest_cache/
dist/
*.egg-info/
docs/sphinx/_build/

# Superpowers working artifacts (specs, plans, reviews) — not part of the code
docs/superpowers/
.claude/

# Design docs — nested git repo with its own history, versioned separately
docs/design/

# Local git worktrees used for isolated subagent-driven development
.worktrees/
benchmarks/
benchmarks_qiskit/

# Lockfile churns heavily with every dependency-group sync; not worth tracking
uv.lock
